server:
  port: 8092

eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
  client:
    serviceUrl:
      defaultZone: http://localhost:8510/eureka/, http://localhost:8511/eureka/

spring:
  application:
    name: spotlight-statistical
  cloud:
    config:
      uri: http://localhost:8001
